apiVersion: v1
kind: Service
metadata:
  name: {{ kustom.name }}
spec:
  ports:
  - name: http
    port: {{ kustom.network.svcPort }}
    protocol: TCP
    targetPort: http
  selector:
    app.kubernetes.io/instance: {{ kustom.name }}
